# :feature:auth

```mermaid
graph TD
    :feature:auth --> :core:navigation
    :feature:auth --> :core:designsystem
    :feature:auth --> :core:domain
    :feature:auth --> :core:common
```

> Note: 위 의존성은 `AndroidFeatureConventionPlugin`을 통해 자동으로 추가됩니다.
